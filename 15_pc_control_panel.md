# PC 上位机控制台

## 1. 作用

`pc/serial_control_panel.py` 是这个项目的最小上位机程序，运行在电脑上，通过串口连接 `STM32 USART2`，用于：

- 手动发送 `UI` / `MV` 调试命令
- 查看 STM32 回传日志
- 观察当前模式、页码、播放状态
- 在没有串口屏或 OpenMV 的时候做联调

## 2. 连接方式

建议连接：

- `STM32 PA2 (USART2_TX)` -> `USB-TTL RX`
- `STM32 PA3 (USART2_RX)` -> `USB-TTL TX`
- `STM32 GND` -> `USB-TTL GND`

波特率：`115200`

## 3. 运行方式

先安装依赖：

```bash
pip install pyserial
```

再运行：

```bash
python D:\26stm_blender\pc\serial_control_panel.py
```

## 4. 当前功能

- 串口连接/断开
- 文献/文物模式切换
- 文献翻页按钮
- 文物播放/重播按钮
- OpenMV 手势模拟按钮
- 原始命令手工发送
- 串口收发日志显示
- 当前模式 / 页码 / 播放状态显示

## 5. 适合的联调用法

### 5.1 替代 TJC 发送命令

直接点击：

- `文献模式`
- `下一页`
- `文献简介`
- `播放`

观察串口屏和 STM32 行为。

### 5.2 替代 OpenMV 发送命令

直接点击：

- `手势上一页`
- `手势下一页`

用于验证 `USART6` 逻辑之外的页面处理是否正确。

### 5.3 手工发原始帧

例如：

```text
<UI:SHOW_DOC_INTRO:1>
<MV:NEXT_PAGE:1>
```

## 6. 当前限制

- 状态显示以 STM32 回传帧和本地推断为主，不是直接读取屏幕状态
- 当前工程已经给 `USART2` 开启接收，电脑既能看日志，也能直接发控制命令
- 如果需要电脑同时“发命令 + 收状态”，STM32 侧必须保留 `USART2` 双向调试逻辑

