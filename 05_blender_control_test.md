# Blender 物品类场景检查

## 1. 目的

这份检查不再用于“实时串口控制 Blender”。

现在它只用于确认：

- 你手动建好的文物模型能接入当前工程
- `RedRelic` 目标对象命名正确
- 后续可以直接用脚本生成 360 度展示动画

## 2. 你现在的正确用法

1. 在 Blender 里手动建模或导入模型
2. 选中整个文物主对象，或建立一个总父对象
3. 把这个主对象命名为：

```text
RedRelic
```

4. 保存场景
5. 运行：

```text
D:\26嵌入式建模\blender\render_turntable_animation.py
```

## 3. 通过标准

运行脚本后应看到：

- 自动创建或复用 `Turntable_Rig`
- 自动创建或复用 `Turntable_Camera`
- 自动布置 3 盏灯
- 给旋转轨道插入 360 度关键帧

如果这些都正常，说明你的手工模型已经成功融入当前工程。

## 4. 常见问题

### 4.1 找不到 `RedRelic`

说明主对象命名不一致。

常见错误：

- `RedRelic.001`
- 中文名
- 只有子零件，没有总父对象

### 4.2 模型位置不对

建议先把主模型中心尽量放在世界原点附近，再运行脚本。

### 4.3 模型能否继续手工修改

可以。

推荐流程是：

1. 先手工建模
2. 再运行脚本布置镜头和灯光
3. 手工微调材质、灯光、构图
4. 最后渲染动画
